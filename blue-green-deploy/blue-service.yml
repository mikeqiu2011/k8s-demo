apiVersion: v1
kind: Service
metadata:
  name: blue-service
spec:
  ports:
    - port: 31001
      nodePort: 31001
      targetPort: nodejs-port
      protocol: TCP
  selector:
    app: helloworld
    ver: blue
  type: NodePort
